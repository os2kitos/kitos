module Kitos.Models.ItProject {
    export interface IItProjectPhase {
        /** Gets or sets the phase name. */
        Name: string;
        /** Gets or sets the phase start date. */
        StartDate: Date;
        /** Gets or sets the phase end date. */
        EndDate: Date;
    }
}
