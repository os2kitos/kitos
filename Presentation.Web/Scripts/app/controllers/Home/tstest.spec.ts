module Tests.Unit.HomeController {
    describe("A controller", () => {
        it("should be true", () => {
            expect(true).toBe(true);
        });

        it("should be false", () => {
            expect(false).toBe(false);
        });
    });
}
